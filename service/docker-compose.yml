version: '2.1'

services:
  gosship:
    build: .
    volumes:
      - db-data:/app/db
    ports:
      - 2222:2222
    mem_limit: 1G
    memswap_limit: 2G
    ulimits:
      core:
        soft: 0
        hard: 0
      nproc:
        soft: 4000
        hard: 4000

volumes:
  db-data:
